@model IEnumerable<EmployeeDto>

<h1>All Employees</h1>
<br />
<br />
<a asp-controller="Employee" asp-action="Create" class="btn btn-primary">Create New Employee</a>

<div class="row justify-content-center align-items-center mt-5">
	<form class="row col-8 offset-2"  asp-action="Index">
		<div class="col-8">
			<input type="text" name="searchInp" class="form-control" placeholder="Search by Name" />
		</div>
		<div class="col-4">
			<input type="submit" value="Search" class="btn btn-primary">
		</div>
	</form>
</div>

@* <div class="mt-5 alert alert-danger"> *@
@* 	<h3>@ViewBag.Message</h3> *@
@* </div> *@

@* <div class="mt-5 alert alert-primary"> *@
@* 	<h3>@ViewData["MessageViewData"]</h3> *@
@* </div> *@

@* <div class="mt-5 alert alert-primary"> *@
@* 	<h3>@TempData["MessageTempData"]</h3> *@
@* </div> *@


@if(Model.Count() > 0)
{
	<table class="table table-striped table-hover">
		<thead>
			<tr>
				<th>@Html.DisplayNameFor(E => E.Name)</th>
				<th>@Html.DisplayNameFor(E => E.Image)</th>
				<th>@Html.DisplayNameFor(E => E.Age)</th>
				<th>@Html.DisplayNameFor(E => E.Address)</th>
				<th>@Html.DisplayNameFor(E => E.Salary)</th>
				<th>@Html.DisplayNameFor(E => E.Email)</th>
				<th>@Html.DisplayNameFor(E => E.PhoneNumber)</th>
				<th>@Html.DisplayNameFor(E => E.HiringDate)</th>
				<th>Details</th>
				<th>Update</th>
				<th>Delete</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var Employee in Model)
			{
				<tr>
					<td>@Employee.Name</td>
					<td>
						<img src="~/Files/Images/@Employee.ImageUrl" width="50" height="50" />
				    </td>
					<td>@Employee.Age</td>
					<td>@Employee.Address</td>
					<td>@Employee.Salary</td>
					<td>@Employee.Email</td>
					<td>@Employee.PhoneNumber</td>
					<td>@Employee.HiringDate</td>

					<partial name="_ActionButtonsPartial" model="@Employee.Id.ToString()" />
				</tr>
			}
		</tbody>
	</table>
}
else
{
	<div class="mt-5 alert alert-warning">
		<h3>No Employees Found</h3>
	</div>
}

